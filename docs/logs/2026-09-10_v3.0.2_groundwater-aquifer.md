# v3.0.2 — World-scale Groundwater & Aquifer

## 目标

v3.0.2 在冻结的 v3.0.1 Subsurface Strata 之上新增 World-scale Groundwater
事实，并将地下水与任意 absolute z 的实际岩层组合成派生 Aquifer 查询。World 层只描述长期、
宏观地下水条件；没有引入地下水流向、压力网络、3D 水格、泉眼、地下河、季节变化或固定活跃
地下水底界。

## 正式输出与输入

`GroundwaterLayer` 只保存三个等长于 Cell Count 的数组和一份 Settings：

- `groundwater_supply: PackedFloat32Array`，范围 `[0, 1]`；
- `water_table_z: PackedFloat32Array`，为 absolute world z；
- `groundwater_salinity_class: PackedInt32Array`，枚举为 `FRESH / BRACKISH / SALINE`；
- `settings: GroundwaterSettings`。

生成输入为 `SpatialGraph`、Final `TerrainHeightLayer`、Final `WorldClimateLayer`、
`WorldHydrologyLayer`、`SurfaceWaterLayer` 和 `GroundwaterSettings`。生成器无 seed，且不依赖
Subsurface Strata；Strata 仅在 Aquifer 查询阶段参与。生成器不修改任何输入。

## 锁定公式

普通陆地的气候补给为：

```text
climate_supply = precipitation / (precipitation + precipitation_reference)
```

每个正式 River Cell 使用现有 `EcologyGenerator.river_strength_for(...)` 作为源强度，按真实
world-space graph edge distance 在 40 范围内传播。每个 Lake Cell 以 1.0 为源强度，在 60
范围内传播。两者均使用：

```text
falloff = 1 - smoothstep(0, radius, distance)
```

多个同类源对目标 Cell 取最大值，不相加；Ocean 不作为 River/Lake recharge 传播通道。
普通陆地最终为：

```text
surface_water_influence = max(river_influence, lake_influence)
groundwater_supply = clamp(climate_supply + 0.30 * surface_water_influence, 0, 1)
```

Ocean 和 Lake 的 `groundwater_supply` 固定为 1。没有用 surface Material permeability、
Ecology moisture、drainage、temperature、evaporation 或 noise 修正补给。

普通陆地地下水位为：

```text
base_depth = lerp(50, 6, sqrt(groundwater_supply))
highland_factor = smoothstep(40, 80, terrain_z)
water_table_depth = base_depth + 20 * highland_factor
water_table_z = terrain_z - water_table_depth
```

Ocean / Lake 的 `water_table_z = terrain_z`。没有额外 clamp 或固定地下 floor。

Marine Influence 由所有 Ocean Cell 作为 distance 0 的 multi-source Dijkstra 源，在真实
world-space graph distance 50 内计算同一 falloff。普通陆地：

```text
salinity_pressure = marine_influence * (1 - groundwater_supply)
pressure < 0.20       -> FRESH
0.20 <= pressure < .60 -> BRACKISH
pressure >= 0.60      -> SALINE
```

Ocean 直接为 `SALINE`，Lake 直接为 `FRESH`。

## Settings 默认值

```text
precipitation_reference = 20.0
river_recharge_radius = 40.0
lake_recharge_radius = 60.0
surface_water_recharge_weight = 0.30
min_water_table_depth = 6.0
max_water_table_depth = 50.0
highland_start_z = 40.0
highland_full_z = 80.0
highland_max_extra_depth = 20.0
active_groundwater_half_depth = 50.0
light_aquifer_threshold = 0.18
heavy_aquifer_threshold = 0.45
marine_influence_radius = 50.0
brackish_salinity_threshold = 0.20
saline_salinity_threshold = 0.60
```

Settings 仅验证当前公式所需的有限值、正半径、合法深度顺序、合法阈值顺序和 `[0, 1]`
recharge weight。

## Aquifer 派生规则

`GroundwaterLayer` 提供 water-table depth、是否位于水位以下、active groundwater、Aquifer
Yield 和 Aquifer Class 查询。水位以下的活跃地下水连续随深度衰减：

```text
depth_below_water_table = water_table_z - z
depth_factor = pow(0.5, depth_below_water_table / 50)
active_groundwater = groundwater_supply * depth_factor
```

水位以上返回 0。查询位置无岩石时 Aquifer Yield 也为 0；否则直接复用当前 RockType 的
`RockCatalog.permeability_for(rock_type)`：

```text
aquifer_yield = clamp(active_groundwater * permeability, 0, 1)
yield < 0.18        -> NONE
0.18 <= yield < .45 -> LIGHT
yield >= 0.45       -> HEAVY
```

Aquifer 不存为 Cell 数组，没有新增 RockType 标签或第二套 permeability 表。

## Debug

World Composition Debug 新增独立 `Groundwater` 页面：

1. Groundwater Supply；
2. Water Table Depth；
3. Groundwater Salinity；
4. Aquifer Class @ Surface -25；
5. Aquifer Class @ z=-25；
6. Aquifer Class @ z=-75。

Supply 使用固定 `[0, 1]` 陆地色阶，Water Table Depth 使用固定参数范围；两者均单独显示
Ocean/Lake。Salinity 与 Aquifer 使用离散类别色。Aquifer slice 将无岩石/高于 terrain 明确显示为
light blue，不混入 NONE Aquifer。

最初补充的 absolute z=0 slice 已替换为 Surface -25。Surface -25 对每个 Cell 使用
`query_z = terrain_z - 25`，表示相对当地 Terrain 的固定深度，用于观察典型浅层地下
Aquifer。选择 25 的依据是标准世界 Water Table Depth P50 约为 24.9。absolute z=-25 / -75
继续保留，用于观察统一 world z 的水平切片。绘制仍保留现有 NO_ROCK 时的 above
terrain / non-rock 安全语义。

Cell Inspector 显示 precipitation、climate supply、River source/strength/influence、Lake
source/influence、surface-water influence、Groundwater Supply、Water Table z/depth、Salinity、
Marine Influence 和 Salinity Pressure；Aquifer slice 额外显示 slice z、水位关系、active
groundwater、Rock Type、permeability、Yield 和 Class。所有中间值只在 Debug 即时计算，不进入
正式 Layer。

## Dedicated tests

命令：

```bash
godot --headless --path . --script tests/worldgen/groundwater/groundwater_generator_test.gd
```

覆盖 Settings、固定公式、world-space recharge 半径与 falloff、多源 max、Ocean 传播阻断、
Ocean/Lake 特例、absolute Water Table、Marine Influence、Salinity 边界、active groundwater
half-depth、Strata/NO_ROCK/Aquifer 联动、Aquifer 阈值、Validator、determinism、输入不变及
Seed 1 / Continents / 20k smoke/stat。

结果：全部 dedicated test groups 通过。

Debug 轻量启动：

```bash
godot --headless --log-file /tmp/tavernvale-v302-debug.log --path . --quit-after 12 \
  tests/worldgen/composition/world_composition_debug_view.tscn
```

结果：退出码 0，无脚本错误。

## Seed 1 / Continents / 20000 Cells 第一轮统计

```text
Land / Ocean / Lake: 8466 / 11234 / 300

Land Groundwater Supply
Min / Mean / P25 / P50 / P75 / P90 / Max
0.0000 / 0.3479 / 0.2135 / 0.3261 / 0.4592 / 0.6116 / 1.0000

Land Water Table Depth
Min / Mean / P25 / P50 / P75 / P90 / Max
6.0000 / 26.0235 / 20.2556 / 24.9253 / 29.7795 / 35.0010 / 70.0000

World Salinity FRESH / BRACKISH / SALINE
6572 / 1957 / 11471

Land Salinity FRESH / BRACKISH / SALINE
6272 / 1957 / 237

Aquifer @ Surface -25 NONE / LIGHT / HEAVY
19640 / 360 / 0

Aquifer @ z=-25 NONE / LIGHT / HEAVY
18585 / 1213 / 202

Aquifer @ z=-75 NONE / LIGHT / HEAVY
18965 / 950 / 85
```

Ocean 11234 个 Cell 全部为 SALINE；Lake 300 个 Cell 全部为 FRESH。

有效 Aquifer 的 RockType 分布：

```text
Surface -25 LIGHT / HEAVY
Limestone 88 / 0
Dolomite 101 / 0
Chalk 24 / 0
Sandstone 146 / 0
Conglomerate 1 / 0

z=-25 LIGHT / HEAVY
Basalt 623 / 202
Dacite 87 / 0
Andesite 223 / 0
Limestone 70 / 0
Dolomite 59 / 0
Chalk 16 / 0
Sandstone 134 / 0
Conglomerate 1 / 0

z=-75 LIGHT / HEAVY
Basalt 527 / 85
Dacite 77 / 0
Andesite 346 / 0
```

固定 absolute slice 下，z=-75 的 LIGHT / HEAVY 均低于 z=-25，体现 active groundwater 的
深度衰减。统计仅用于记录 RockType 迁移后的结果，未据此自动调参。

## 版本边界

原始 v3.0.2 实现没有修改当时 v3.0.1 Subsurface Strata 的算法、参数、数据结构或输出，
也没有修改当时的 Material permeability。随后 v3.0.1.1 将岩性接口迁移为 RockType；
Groundwater 的供水、水位、盐度、衰减、Aquifer 公式、阈值和全部 v3.0.2 参数均未修改。

后续 Debug 补充将 absolute z=0 替换为 Aquifer Class @ Surface -25，并更新 Inspector 与
对应统计/测试；没有修改任何 v3.0.2 正式生成算法、正式数据结构、Aquifer/Salinity 规则或
参数。
